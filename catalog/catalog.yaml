apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: feathers-app
  description: Example feathersjs app
  tags:
    - app-accelerator
    - node
    - express
    - tanzu
  annotations:
    'backstage.io/kubernetes-label-selector': 'app.kubernetes.io/part-of=feathers-app'
spec:
  type: service
  lifecycle: experimental
  owner: default-team
